{
  "name": "@sidra/bridge",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "smoke": "vitest run src/bridge.smoke.test.ts"
  },
  "dependencies": {
    "@sidra/protocol": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.0.15"
  }
}
